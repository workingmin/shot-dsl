{
  "name": "shot-dsl-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Internal text-to-wireframe shot prototype based on Storyboarder's historical STS.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/serve.mjs",
    "smoke": "node scripts/browser-smoke.mjs",
    "test": "node --test prototype/src/*.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "esbuild": "0.28.1",
    "events": "3.3.0"
  },
  "engines": {
    "node": ">=22"
  }
}
