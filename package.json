{
  "name": "shot-dsl-previs",
  "version": "0.7.0",
  "private": true,
  "description": "Text-driven 3D dynamic storyboard previs prototype.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/serve.mjs",
    "assets:audit": "node scripts/audit-character-assets.mjs",
    "test": "node --test",
    "smoke": "node scripts/browser-smoke.mjs",
    "check": "npm run assets:audit && npm test && npm run build"
  },
  "dependencies": {
    "esbuild": "0.25.8",
    "three": "0.185.0"
  },
  "engines": {
    "node": ">=22"
  }
}
